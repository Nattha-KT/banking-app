import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* For a tip how to silence the warnings. " [webpack.cache.PackFileCacheStrategy]" */
//   webpack: (config) => {
//     config.infrastructureLogging = {
//         level: "error",
//     };

//     return config;
// },
};

export default nextConfig;
